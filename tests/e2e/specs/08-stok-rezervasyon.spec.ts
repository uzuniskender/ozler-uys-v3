import { test, expect } from '../fixtures';

test.describe('Stok Rezervasyon', () => {
  test.fixme('Rezervasyon oluşturma: İş emri için stok rezerve edilebilmeli', async () => {});
  test.fixme('Müsait stok: Rezerv edilen miktar toplam stoktan düşülmeli', async () => {});
  test.fixme('Çift rezervasyon engeli: Yetersiz stokta rezerv reddedilmeli', async () => {});
  test.fixme('Rezerv iptali: İptal sonrası miktar tekrar müsait görünmeli', async () => {});
  test.fixme('İş emri silinince rezerv otomatik iptal olmalı (CASCADE)', async () => {});
  test.fixme('Rezerv listesi: Planlama rolü aktif rezervleri görebilmeli', async () => {});
});
